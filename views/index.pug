extends layout.pug

block content
  article.grid-container.portfolio-intro
    div

      h1 Welcome to My Portfolio!!

      p.lead.text-light
        | Below is a small selection of software projects I've recently completed, which includes a web project built in Rails, data-science project built in R, and mobile application platform developed in Filemaker Pro. Stay tuned for a ton more modern Javascript, REACT, and Node projects coming soon....
  article.grid-container.portfolio-index
    
    .grid-x.grid-margin-x.small-up-2.medium-up-2.large-up-3

      each project in projects
        .cell
          a(href=`/project/${project.id}`)
            img.thumbnail(src=`static/img/thumb_${project.id}.png`)
            h5= project.name
